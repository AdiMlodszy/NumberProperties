#ifndef UTILS_H
#define UTILS_H

#include <stdbool.h>

// Deklaracje funkcji sprawdzających właściwości liczb
bool is_palindromic(int number);
bool is_perfect(int number);
bool is_harshad(int number);
bool is_happy(int number);
bool is_armstrong(int number);

#endif // UTILS_H
